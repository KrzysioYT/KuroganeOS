name: Apply 4.0 Driver Manager 2.0 isolation

on:
  push:
    branches:
      - gpt/road-to-15-consolidation
    paths:
      - '.github/workflows/apply-driver-manager-v2.yml'
      - '.github/scripts/apply-driver-manager-v2.py'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential binutils python3

      - name: Apply Driver Manager 2.0 isolation
        run: |
          python3 -m py_compile .github/scripts/apply-driver-manager-v2.py
          python3 .github/scripts/apply-driver-manager-v2.py

      - name: Compile release kernel userspace SDK and boot chain
        run: bash ./scripts/build-linux.sh --configuration release --rebuild --no-image

      - name: Commit compiled Driver Manager 2.0 isolation
        run: |
          rm -f .github/workflows/apply-driver-manager-v2.yml
          rm -f .github/scripts/apply-driver-manager-v2.py
          git config user.name 'KuroganeOS CI'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'drivers(core): add Driver Manager 2.0 failure isolation'
          git push origin HEAD:gpt/road-to-15-consolidation
