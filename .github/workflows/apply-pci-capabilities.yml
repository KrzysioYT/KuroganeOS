name: Apply 5.0 PCI capability discovery

on:
  push:
    branches:
      - gpt/road-to-15-consolidation
    paths:
      - '.github/workflows/apply-pci-capabilities.yml'
      - '.github/scripts/apply-pci-capabilities.py'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential binutils python3

      - name: Apply PCI capability discovery
        run: |
          python3 -m py_compile .github/scripts/apply-pci-capabilities.py
          python3 .github/scripts/apply-pci-capabilities.py

      - name: Compile release kernel userspace SDK and boot chain
        run: bash ./scripts/build-linux.sh --configuration release --rebuild --no-image

      - name: Commit compiled PCI capability discovery
        run: |
          rm -f .github/workflows/apply-pci-capabilities.yml
          rm -f .github/scripts/apply-pci-capabilities.py
          git config user.name 'KuroganeOS CI'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'pci: add bounded MSI MSI-X capability discovery'
          git push origin HEAD:gpt/road-to-15-consolidation
