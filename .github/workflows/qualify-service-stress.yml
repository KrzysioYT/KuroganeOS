name: Qualify Service Channel Churn

on:
  push:
    branches:
      - gpt/road-to-15-consolidation
    paths:
      - '.github/workflows/qualify-service-stress.yml'
      - 'userspace/system/service-stress-probe/main.c'
      - 'userspace/system/service-meta-server/main.c'
      - 'sdk/include/kurogane/service.h'
      - 'sdk/include/kurogane/syscall.h'
      - 'kernel/ipc/channel.cpp'
      - 'kernel/ipc/channel.hpp'
      - 'kernel/user/runtime.cpp'

jobs:
  service-channel-churn:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout tested commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          submodules: recursive

      - name: Inject stress binaries and launch
        shell: python
        run: |
          from pathlib import Path

          build = Path('scripts/build-linux.sh')
          text = build.read_text()
          anchor = '    "clipboardprobe|userspace/system/clipboard-probe/main.c|system/clipprb|c"\n'
          addition = (
              '    "svcmeta|userspace/system/service-meta-server/main.c|system/svcmeta|c"\n'
              '    "svcstress|userspace/system/service-stress-probe/main.c|system/svcstress|c"\n'
          )
          if text.count(anchor) != 1:
              raise SystemExit(f'stress build anchor count={text.count(anchor)}')
          build.write_text(text.replace(anchor, anchor + addition, 1))

          init = Path('userspace/system/init/main.c')
          text = init.read_text()
          anchor = '    (void)u_puts("[TEST] clipboard_service_spawn: PASS\\n");\n'
          addition = anchor + (
              '    const ku_result_t stress_service_pid = u_spawn("/system/svcmeta");\n'
              '    if (stress_service_pid <= 0) {\n'
              '        (void)u_puts("[TEST] service_channel_churn: FAIL\\n");\n'
              '        ku_exit(33);\n'
              '    }\n'
              '    const ku_result_t stress_probe_pid = u_spawn("/system/svcstress");\n'
              '    if (stress_probe_pid <= 0) {\n'
              '        (void)u_puts("[TEST] service_channel_churn: FAIL\\n");\n'
              '        ku_exit(34);\n'
              '    }\n'
          )
          if text.count(anchor) != 1:
              raise SystemExit(f'stress init anchor count={text.count(anchor)}')
          init.write_text(text.replace(anchor, addition, 1))

      - name: Validate stress patch
        run: |
          git diff --check
          grep -F 'STRESS_ITERATIONS 256U' userspace/system/service-stress-probe/main.c
          grep -F 'system/svcmeta' scripts/build-linux.sh
          grep -F 'system/svcstress' scripts/build-linux.sh
          grep -F 'service_channel_churn: FAIL' userspace/system/init/main.c

      - name: Install build and QEMU dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential binutils python3 qemu-system-x86 ovmf mtools dosfstools gdisk xorriso

      - name: Build full release media
        run: |
          chmod +x scripts/*.sh
          bash ./scripts/build-media-linux.sh --configuration release --rebuild
          test -s build/images/KuroganeOS-linux.img

      - name: Require bounded service channel churn
        run: |
          bash ./scripts/smoke-uefi-iso-qemu.sh \
            build/images/KuroganeOS-linux.img \
            --disk \
            --require-marker '[TEST] service_channel_churn: PASS' \
            --timeout 180
