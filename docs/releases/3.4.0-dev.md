# KuroganeOS 3.4.0-dev

- **Version:** 3.4.0-dev
- **Channel:** DEV
- **Release status:** IN DEVELOPMENT / NOT RELEASED
- **Date:** 2026-08-29
- **Generation:** Red Flux services foundation

## Summary

3.4.0-dev begins the userspace service architecture without introducing a
parallel fake registry. The existing kernel named-IPC endpoint table remains
the source of truth for service names, PID ownership, generation-safe handles,
pending connections and process-exit cleanup. A public service-oriented SDK
surface now exposes that real backend as service registration and discovery by
connection.

Formal sequential release state remains blocked at 3.3.4-dev pending real
Oracle VirtualBox host acceptance. This file therefore describes stacked
development and is not a release tag.

## Kernel changes

No new kernel transport was required for the first 3.4.0 atom. Existing IPC
already provides bounded named endpoints and per-process ownership, and the
runtime releases IPC objects on process exit.

## Driver changes

None in this atom.

## Userspace changes

Added `sdk/include/kurogane/service.h` with typed service operations:

- `ku_service_register`
- `ku_service_connect`
- `ku_service_accept`
- `ku_service_send`
- `ku_service_receive`
- `ku_service_close`

The functions delegate directly to the existing real IPC syscall transport.
They do not return fabricated success and do not maintain duplicate service
state.

## Security changes

Service endpoint ownership continues to be enforced by the existing IPC PID
checks and generation-safe handles. Process exit continues to invoke IPC
resource release.

## Networking changes

None.

## Graphics changes

None.

## Filesystem changes

None.

## Application changes

None yet. This atom establishes the API used by future userspace services.

## ABI changes

No syscall number or ABI version is changed in this atom. The service API is a
typed source-level layer over existing public IPC syscalls. ABI regression tests
lock service handle/message types and capacities to the underlying IPC ABI.

## Breaking changes

None.

## Build status

Implementation head before qualification workflow update:
`eda5695b8237a0a0edb95f8c565434cdb4ab54d7`

Qualification head:
`716d2cac9c49a27d7e8cf11f813de56b9d29f256`

## Test matrix

### PASS so far

- Kernel test configuration compile in run `33221125505`.
- Host ABI/SDK regression in run `33221125505`, including the new service ABI
  aliases.

### IN PROGRESS

- Full host regression, media, FAT32/VFS, verifier, OVMF and QEMU network matrix
  in run `33221125505`.

### PENDING

- Completion of run `33221125505` before marking the service API atom TESTED.
- Real Oracle VirtualBox release acceptance remains an earlier external gate.

## Virtualization support

No virtualization claim is changed by the service API itself. The branch uses
the same full UEFI/QEMU qualification workflow as the 3.3.9 closeout.

## Real hardware status

PENDING. No new real-hardware claim is made.

## Migration notes

Applications using raw `ku_ipc_*` APIs remain compatible. `ku_service_*` is an
additive service-oriented API and does not deprecate raw IPC.

## Remaining limitations

- Service metadata enumeration/version negotiation is not yet implemented.
- Event broker/subscription semantics belong to 3.4.1 and are not claimed here.
- Service restart/recovery belongs to later 3.4.x work.
- Formal 3.3.4 release closure still requires real Oracle VirtualBox acceptance.
