KuroganeOS runtime variable-data directory.

This directory is intentionally present in the installed root filesystem so
runtime services and persistence probes have a writable parent directory.
Persistent state files created here are not installation payload files.
